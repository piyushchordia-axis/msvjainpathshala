# Local development config for `pnpm --filter @jp/web dev`.
# The backend reads its own port from apps/api/.env.development (currently 3005).
INTERNAL_API_BASE_URL=http://localhost:3000
NEXT_INTERNAL_API_BASE_URL=http://localhost:3000
