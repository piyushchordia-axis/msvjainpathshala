{
  "name": "@jp/eslint-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared ESLint flat config used across Jain Pathshala apps (built in Step 2).",
  "scripts": {
    "lint": "echo \"@jp/eslint-config: lint not applicable (config-only package)\" && exit 0",
    "typecheck": "echo \"@jp/eslint-config: typecheck not yet configured (Step 2)\" && exit 0"
  }
}
