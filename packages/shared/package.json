{
  "name": "@jp/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas, enums, error codes, and types across Jain Pathshala apps (built in Step 2).",
  "scripts": {
    "lint": "echo \"@jp/shared: lint not yet configured (Step 2)\" && exit 0",
    "typecheck": "echo \"@jp/shared: typecheck not yet configured (Step 2)\" && exit 0"
  }
}
