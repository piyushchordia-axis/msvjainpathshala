import CourseAdmin from "@/components/CourseAdmin";

/** Q12 — sanchalak certification must ship with the shikshak batch-bound gate. */
export default function AdminCoursesScreen() {
  return <CourseAdmin persona="sanchalak" />;
}
