/**
 * Exam jobs — stubs until exam-attempt-abandon / exam-punya land in-tree.
 * Full implementation lives in WIP; keep exports so index.ts imports succeed.
 */
export function registerExamJobs(): void {
  // no-op: job modules not present on this branch tip yet
}