import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ParentChildren() {
  return (
    <PlaceholderScreen
      title="My children"
      emptyBody="Your children's enrolment, ID cards, and Punya tiers. Lands when the enrolment step ships."
    />
  );
}
