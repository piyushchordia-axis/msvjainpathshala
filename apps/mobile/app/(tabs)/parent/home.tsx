import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ParentHome() {
  return (
    <PlaceholderScreen
      title="Home"
      subtitle="Today's at-a-glance"
      emptyBody="Your children's recent attendance, Punya progress, and unread notices. Lands in the engagement step."
    />
  );
}
