import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ParentLibrary() {
  return (
    <PlaceholderScreen
      title="Library"
      emptyBody="Audio, videos, and PDFs for your family. Lands when the library step ships."
    />
  );
}
