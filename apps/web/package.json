{
  "name": "@jp/web",
  "version": "0.0.0",
  "private": true,
  "description": "Jain Pathshala — Next.js 15 public website + admin panel (placeholder; built in later step).",
  "scripts": {
    "lint": "echo \"@jp/web: lint not yet configured\" && exit 0",
    "typecheck": "echo \"@jp/web: typecheck not yet configured\" && exit 0"
  }
}
