import { PageStub } from '@/components/public/PageStub';

export default function NoticesPage() {
  return (
    <PageStub
      kicker="Notices"
      title="Notices from the MSV network"
      body="Public notices and critical announcements. Lands with the notices step."
    />
  );
}
