import { PageStub } from '@/components/public/PageStub';

export default function GalleryPage() {
  return (
    <PageStub
      kicker="Gallery"
      title="Moments from across the network"
      body="Photos and videos shared by sanchalaks (only from families who have opted in). Lands with the gallery step."
    />
  );
}
