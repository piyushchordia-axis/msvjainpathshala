import { PageStub } from '@/components/public/PageStub';

export default function CentresPage() {
  return (
    <PageStub
      kicker="Centres"
      title="Find a Pathshala near you"
      body="Browse the MSV centre directory with map view, sanchalak contact, batch schedule, and capacity. Lands when the centres directory step ships."
    />
  );
}
