import { PageStub } from '@/components/public/PageStub';

export default function LibraryPage() {
  return (
    <PageStub
      kicker="Library"
      title="Audio, video and reading"
      body="Curated audio talks, embedded videos, and downloadable PDFs. Lands with the library step."
    />
  );
}
